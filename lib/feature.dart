import 'package:flutter/material.dart';

class MockFeature extends StatelessWidget {
  const MockFeature({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
